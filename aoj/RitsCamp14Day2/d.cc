#include<iostream>
#include<map>
#include<vector>
#include<algorithm>
#include<cmath>
#include<iomanip>
#include<numeric>
#include<climits>
#include<ctime>
#include<cstring>
#include<sstream>
#define REP(i,p,n) for(int i=p;i<(int)(n);i++)
#define rep(i,n) REP(i,0,n)
#define ALL(c) (c).begin(), (c).end()
#define dump(a) cerr << #a << "=" << (a) << endl
#define DUMP(list) cout << "{ "; for(auto nth : list){ cout << nth << " "; } cout << "}" << endl;

using namespace std;

/*
map<vector<int>,char> nums = {
	{{7,5,5,5,7},'0'},
	{{4,4,4,4,4},'1'},
	{{7,1,7,4,7},'2'},
	{{7,1,7,1,7},'4'},
	{{7,1,7,1,7},'5'},
	{{7,1,7,1,7},'6'},
	{{7,1,1,1,1},'7'},
	{{7,5,7,5,7},'8'},
	{{7,5,7,1,7},'9'},
	{{0,2,7,2,0},'+'},
	{{0,0,7,0,0},'-'},
	{{0,0,1,0,0},'*'},
};
*/

int main() {
	int N,x1,y1,x2,y2;
	cin >> N;
	rep(i,N) {
		cin >> x1 >> y1 >> x2 >> y2;
		rep() {
		}
	}

	return EXIT_SUCCESS;
}
