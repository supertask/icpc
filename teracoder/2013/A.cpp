#include<iostream>
#include<cstdlib>
using namespace std;

int main() {
	string c;
	string T;
	string name;

	cin >> c;
	while (cin >> T) {
		cout << T << endl;
		if (T == "#") { break; }
		
		if (T.c_str()+1 == )
		
		
		
		
		for (int t=0; t < atoi(T.c_str()); t++) {
			cin >> name;
			if (name == c) {
				cout << name;
			}
			//cout << name << endl;
		}
	}
	return 0;
}